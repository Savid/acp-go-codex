---
title: "Updates"
description: "Codex events mapped to ACP updates."
---

Codex app-server events are mapped to ACP session updates for agent messages,
reasoning, plans, tool calls, diffs, usage, and account metadata.

Raw rollout rows are emitted only through `_codex/rawEvent` when a session opts
in with `_meta.codex.rawEvent.enabled`.
